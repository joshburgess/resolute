/target
/.bench-results
# sqlx's offline cache. Resolute uses `.resolute/` instead; sqlx is only a
# bench-side runtime dependency and never produces this directory itself,
# but `cargo sqlx prepare` would create it if someone ran it here.
/.sqlx
